create database `pedro`;
# a linha acima serve para criar um banco de dados com o nome: `pedro`
# o uso da crase se torna necessário para indicar um `string` do sistema
# `strings` do sistema: `database`, `table` e `campo` 